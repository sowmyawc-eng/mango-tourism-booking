rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Open rules — all reads and writes allowed.
    // Deploy with: firebase deploy --only firestore:rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
